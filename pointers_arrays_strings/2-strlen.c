#include "main.h"

/**
 *_strlen - Entry point
 *@s: is to char type
 *Description: returns the length of
 *string
 *Return: l length of a string
 */

int _strlen(char *s)
{
	int l;

	for (l = 0 ; s[l] ; l++)
	{

	}
		return (l);
}
