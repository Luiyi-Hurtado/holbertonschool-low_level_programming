#include "main.h"

/**
*_isupper - Entry pint
*@c: is a type int
*Description: the script returns 1 if the letters
*is to uppercase and returns 0 if is otherwise
*Return: 0
*/
int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
		return (1);
	}

return (0);
}
